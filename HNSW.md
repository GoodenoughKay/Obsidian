好，整理如下。

---

# HNSW 项目复试问答整理

## 自我介绍（HNSW 部分）

> HNSW 是一种近似最近邻搜索算法，用于在大规模向量数据库中快速找到相似向量。我的毕设是基于 C++ 针对 Apple M3 芯片实现的 HNSW 向量搜索引擎。不同于通用实现，我专门针对 ARM 架构做了深度硬件优化：用 NEON SIMD 加速距离计算，FP16 压缩数据提升并行效率，自旋锁替换 mutex 减少上下文切换，DMP 硬件预取提升访存效率。最终在 SIFT1M 百万级数据集上，单核构建吞吐量提升 3.2 倍，8线程构建加速比 3.31 倍，查询 QPS 7104，单次延迟 0.14 毫秒，Recall@10 达 92.4%。

---

## Q1：HNSW 是什么，解决什么问题？

> HNSW 全称 Hierarchical Navigable Small World graphs，解决的是近似最近邻搜索问题——在大规模向量数据库里，给一个查询向量，快速找到和它最相似的若干个向量。通过分层图结构把搜索复杂度从暴力搜索的 $O(n)$ 降到 $O(\log n)$。

---

## Q2：HNSW 的分层图结构怎么理解？

> 可以理解成朋友圈。顶层节点少但跨度大，像是人脉很广的超级节点，用来快速定位大致区域；底层包含所有节点，连接最密集，用来精确搜索。搜索从顶层开始，快速定位，逐层下降到底层找到最近邻。和 Adam + L-BFGS 两阶段优化的逻辑很像，先粗定位再精搜索。

---

## Q3：NEON SIMD 是什么，在你项目里做了什么？

> NEON 是 ARM 架构的 SIMD 指令集，SIMD 是单指令多数据。普通加法一次算一个浮点数，NEON 一次可以同时算4个或8个浮点数。在我的项目里，向量距离计算用的是欧氏距离的平方，是最频繁的操作。用 NEON 替换普通标量计算，直接把计算吞吐量翻了几倍，这是单核构建吞吐量提升 3.2 倍的主要原因之一。

---

## Q4：FP16 压缩有什么代价？为什么可以接受？

> 代价是精度损失，32位变16位，小数点后精度变低。但我做的是近似最近邻搜索，本来就不要求绝对精确，92.4% 的召回率证明精度损失在可接受范围内。另外 FP16 比 FP32 小一半，NEON 寄存器能装下两倍的数据，SIMD 并行效率更高，这也是使用 FP16 的额外好处。

---

## Q5：为什么用自旋锁替换 mutex？

> mutex 抢不到锁时线程会进入内核态休眠，用户态到内核态的上下文切换开销很大，大概几百到几千个时钟周期。自旋锁抢不到锁时在用户态原地等待，不切换内核态。在 HNSW 构建过程中，锁的持有时间很短，只是更新一个邻居列表，自旋等待的总开销远小于内核切换的开销。但如果持有锁的时间很长，自旋锁反而更差，因为违反了让权等待原则，会浪费大量 CPU 资源。

---

## Q6：3.2 倍提升是和什么对比的？

> 我实现了一个朴素的 baseline 版本作为对照，baseline 用纯标量 float32 计算、std::mutex 锁、没有任何硬件优化。优化版加入 NEON SIMD、FP16、自旋锁等优化后，在相同数据集上对比测试，单核构建吞吐量提升了 3.2 倍。

---

## Q7：为什么选 SIFT1M 数据集？

> SIFT1M 是向量搜索领域的标准 benchmark，包含 100 万个 128 维的图像特征向量，SIFT 是尺度不变特征变换提取的图像特征。选它是因为业界通用，用它测试的结果可以和其他 HNSW 实现横向对比，有说服力。

---

## Q8：和 hnswlib 开源库相比优势在哪里？

> hnswlib 是通用实现，能跑在各种平台上，但没有针对特定硬件深度优化。我的版本专门针对 Apple M3 芯片，利用了 NEON SIMD、FP16、DMP 硬件预取、自旋锁等 ARM 架构特有的硬件特性，在 M3 上的实际性能远高于直接跑 hnswlib。

---

## 必须背下来的数字

|内容|数字|
|---|---|
|单核构建吞吐量提升|3.2 倍|
|8线程构建加速比|3.31 倍|
|查询 QPS|7104|
|单次延迟|0.14 毫秒|
|Recall@10|92.4%|
|数据集规模|100万，128维|

---

## 三个容易说错的地方

1. SIMD 是"**单**指令多数据"，不是"当指令"
2. NEON、FP16、DMP 是"**硬件**特性"，不是"软件特性"
3. 自旋锁适合**持锁时间短**的场景，持锁时间长反而更差

---

保存好，和 PINN 那份一起每天读一遍。有复试消息了来找我继续准备。
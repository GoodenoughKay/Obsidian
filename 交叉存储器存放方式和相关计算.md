好,笔记里有交叉存储器的核心内容。我来系统地展开,把**存放方式**和**所有常考计算**讲透。

---

# 交叉存储器专题

## 🧠 先搞懂为什么需要它

一个存储模块的存储周期是 $T$,在这段时间内 CPU 只能干等。如果把主存拆成多个**独立模块**(每个有自己的地址寄存器、数据寄存器),就可以让多个模块**流水线式**交替工作,大幅提升吞吐率。

关键问题就是:**地址怎么分给各模块?**

---

## 📊 两种编址方式对比

### ① 高位交叉(顺序编址)

**地址的高位决定模块号,低位是模块内地址。**

$$\underbrace{A_{n-1}\cdots A_{n-k}}_{\text{模块号(高位)}} \quad \underbrace{A_{n-k-1}\cdots A_0}_{\text{模块内地址(低位)}}$$

以 4 个模块为例,每个模块有 4 个地址:

```
模块 M0: 地址 0,  1,  2,  3
模块 M1: 地址 4,  5,  6,  7
模块 M2: 地址 8,  9, 10, 11
模块 M3: 地址 12, 13, 14, 15
```

**存放规律**:每个模块内的地址是**连续**的,像把一本书按章节分给不同书架。

**致命缺点**:程序通常是顺序访问连续地址(0→1→2→3),这些地址都在**同一个模块**里!其他模块全部空闲,流水线根本启动不了,和单模块没区别。

**唯一用途**:方便扩展(增加模块就能扩容),但对提速**毫无帮助**。

---

### ② 低位交叉(交叉编址)⭐ 重点!

**地址的低位决定模块号,高位是模块内地址。**

$$\underbrace{A_{n-1}\cdots A_k}_{\text{模块内地址(高位)}} \quad \underbrace{A_{k-1}\cdots A_0}_{\text{模块号(低位)}}$$

同样 4 个模块,每个模块 4 个地址:

```
模块 M0: 地址 0,  4,  8, 12    (所有 mod 4 = 0 的地址)
模块 M1: 地址 1,  5,  9, 13    (所有 mod 4 = 1 的地址)
模块 M2: 地址 2,  6, 10, 14    (所有 mod 4 = 2 的地址)
模块 M3: 地址 3,  7, 11, 15    (所有 mod 4 = 3 的地址)
```

**存放规律**:连续地址**轮流分布**在不同模块中,像发扑克牌一样一张一张轮着发。

$$\text{地址 } addr \text{ 所在模块号} = addr \mod m$$

**核心优势**:程序顺序访问 0→1→2→3 时,每个地址在**不同模块**,四个模块可以**流水线并行工作**!

---

## 🔑 流水线工作原理(这是计算的基础)

这是整个交叉存储器的精髓,用一张时间图彻底看懂:

设 4 个模块,存储周期 $T = 200,ns$,总线传输时间 $\tau = 50,ns$。

```
时间轴(ns): 0    50   100  150  200  250  300  350
            ├────┼────┼────┼────┼────┼────┼────┤
M0:         |████████████████████|              ← 存取周期 T=200ns
            |读M0              |完成,数据上总线
M1:              |████████████████████|
                 ↑启动M1(延迟τ=50ns)
M2:                   |████████████████████|
M3:                        |████████████████████|

总线:       |取M0|取M1|取M2|取M3|
            0   50  100  150  200
```

**关键理解**:

第一,M0 启动后,**不用等 M0 完成**,隔 $\tau$ 时间就启动 M1。因为 M0 在内部默默读数据(需要 T 时间),不占总线。

第二,M0 读完后把数据放到总线上,占用总线 $\tau$ 时间。此时 M1 恰好也读完了,接着上总线。

第三,所以**连续访问 $m$ 个地址**的总时间 = $T + (m-1) \times \tau$。第一个等 $T$,后面每个只等 $\tau$。

---

## 📐 核心公式(必须背)

### 公式 1:连续访问 $m$ 个模块的总时间

$$t_{总} = T + (m-1)\tau$$

### 公式 2:最优模块数

$$m_{最优} = \frac{T}{\tau}$$

当 $m = T/\tau$ 时,流水线**完美衔接**——M0 完成存取的瞬间,数据立即上总线,接着 M1 的数据紧跟其后,没有任何空闲等待。

### 公式 3:带宽计算

**单模块带宽**(每 $T$ 时间传一个字):

$$W_0 = \frac{W}{T}$$

其中 $W$ 是每次传输的数据量(字节)。

**$m$ 模块交叉带宽**(连续取 $m$ 个字):

$$W_m = \frac{m \times W}{T + (m-1)\tau}$$

**最优情况**(m = T/τ 时):

$$W_{最优} = \frac{W}{\tau}$$

每隔 $\tau$ 就出一个数据,带宽提升到单模块的 $T/\tau$ 倍。

### 公式 4:连续存取 $n$ 个字的总时间

当 $n > m$ 时(要取的数据超过模块数),流水线会"转圈":

$$t_{总} = T + (n-1)\tau$$

这个公式只在**最优模块数**($m = T/\tau$)时成立。否则中间会有气泡(等待)。

---

## 🎯 典型题型与完整例题

### 题型一:带宽计算(最高频)

**例题**:主存由 4 个模块低位交叉编址,存储周期 $T = 200,ns$,总线传输时间 $\tau = 50,ns$,每次传 4 字节。求带宽并与单模块比较。

**解答**:

单模块带宽:$W_0 = \dfrac{4,B}{200,ns} = 20,MB/s$

4 模块交叉: $W_4 = \dfrac{4 \times 4,B}{200 + 3 \times 50} = \dfrac{16}{350},B/ns \approx 45.7,MB/s$

最优模块数 $m^* = 200/50 = 4$,恰好等于实际模块数,所以达到最优:

$W_{最优} = \dfrac{4,B}{50,ns} = 80,MB/s$

⚠️ 这里有两种带宽算法:$45.7,MB/s$ 是"首次启动到 4 个全读完"的**平均带宽**;$80,MB/s$ 是**稳态带宽**(流水线满载后每 $\tau$ 出一个数据)。题目问什么算什么,一般问"连续存取 $m$ 个字的带宽"用前者,问"持续流水带宽"用后者。

---

### 题型二:连续取 n 个字的总时间

**例题**:某低位交叉存储器有 4 个模块,$T = 80,ns$,$\tau = 20,ns$。连续读出 8 个字,需要多长时间?

**解答**:

验证:$m = T/\tau = 80/20 = 4$ ✅ 最优

$$t_{总} = T + (n-1)\tau = 80 + 7 \times 20 = 80 + 140 = 220,ns$$

对比单模块:$8 \times 80 = 640,ns$。交叉存储器快了约 **3 倍**。

---

### 题型三:最优模块数反推

**例题**:存储周期 $T = 100,ns$,总线周期 $\tau = 25,ns$,要使低位交叉存储器流水线不产生空闲等待,至少需要几个模块?

**解答**:

$$m \geq \frac{T}{\tau} = \frac{100}{25} = 4 \text{ 个模块}$$

模块数少于 4:上一轮 M0 还没完成,下一轮又要访问 M0,就得**等待**,流水线出现"断流"。

模块数多于 4:多出来的模块在流水线中不会被用到(连续访问时只轮到前 $m$ 个),浪费硬件。

所以**恰好 4 个**最经济。

---

### 题型四:地址所在模块判断

**例题**:4 模块低位交叉存储器,地址 $25$ 在哪个模块?

$$25 \mod 4 = 1 \quad \Rightarrow \quad \text{模块 } M_1$$

**例题**:4 模块高位交叉存储器,每模块 $1K$ 个地址,地址 $2049$ 在哪个模块?

$$\lfloor 2049 / 1024 \rfloor = 2 \quad \Rightarrow \quad \text{模块 } M_2$$

---

### 题型五:综合题(真题风格)

**例题**(2015 真题风格):某计算机主存地址空间 $64K$ 字,采用 4 体低位交叉存储器,存储周期 $T = 200,ns$,数据总线宽度 32 位,总线传输周期 $\tau = 50,ns$。

1. 每个模块的容量是多少?
2. 地址如何划分?
3. 连续存取 4 个字的时间是多少?

**解答**:

**(1)** 每个模块容量 = $64K / 4 = 16K$ 字

**(2)** $64K = 2^{16}$ → 地址共 16 位。4 个模块 → 低 2 位做模块号:

$$\underbrace{A_{15}\sim A_2}_{14\text{位:模块内地址}} \quad \underbrace{A_1 A_0}_{2\text{位:模块号}}$$

|$A_1A_0$|模块|
|---|---|
|00|M0|
|01|M1|
|10|M2|
|11|M3|

**(3)** 连续取 4 个字:

$$t = T + (4-1)\tau = 200 + 3 \times 50 = 350,ns$$

---

## 🚨 高频踩坑点

**坑 1:高位交叉 ≠ 交叉存储器**

考试说"交叉存储器"默认指的是**低位交叉**。高位交叉本质上就是普通的"顺序编址",没有流水线加速效果。如果选择题问"交叉存储器能提高带宽",说的一定是低位交叉。

**坑 2:模块数不够时的等待**

如果 $m < T/\tau$,流水线会"断流"。比如 $T = 200,ns$,$\tau = 50,ns$ 但只有 3 个模块:

```
M0: |████████████████████|        |████████████████████|
M1:      |████████████████████|
M2:           |████████████████████|
        ← 150ns后要访问M0,但M0还没完成(需200ns)!
        → 必须等50ns → 流水线有气泡
```

连续取 $n$ 个字的总时间不再是 $T + (n-1)\tau$,而是更长。

**坑 3:分清"存储周期 T"和"总线传输周期 τ"**

$T$ 是**模块内部**从接收地址到数据稳定输出的时间(几百 ns)。$\tau$ 是数据通过**总线**传到 CPU 的时间(几十 ns)。$T \gg \tau$,正是这个速度差才让流水线有意义。

**坑 4:高位交叉的带宽**

高位交叉连续访问时**没有加速**,带宽就等于单模块带宽 $W/T$。但如果多个进程**同时访问不同模块**(多体并行),高位交叉也能提速——不过这不是考试重点。

**坑 5:"存取 n 个字"公式的前提**

$t = T + (n-1)\tau$ 这个公式有两个前提:一,低位交叉;二,$m \geq T/\tau$(模块数足够)。如果题目没满足这两个条件,不能直接套公式。

---

## 📝 快速对比总结

| 对比项        | 高位交叉(顺序编址)   | 低位交叉(交叉编址)      |
| ---------- | ------------ | --------------- |
| 模块号由谁决定    | 地址**高**位     | 地址**低**位        |
| 连续地址分布     | 在**同一个**模块内  | **轮流**分布在不同模块   |
| 能否流水线加速    | ❌ 不能         | ✅ 能             |
| 连续取 m 个字时间 | $m \times T$ | $T + (m-1)\tau$ |
| 带宽提升       | 无            | 最高提升 $T/\tau$ 倍 |
| 扩容方便性      | ✅ 直接加模块      | ⚠️ 模块数改变后地址映射全变 |
| 实际用途       | 扩容           | **提速(考试重点)**    |

---

需要我再出几道变式练习题吗?比如"模块数不够导致断流"的时间计算,或者"低位交叉+Cache"的综合题,这些是真题里经常组合出现的。